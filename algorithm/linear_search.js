// Linear Search Algorithm/ O(n)

// const friends = ['Nayeem', 'Tumpa', 'Mafruha', 'Urmi', 'Pollob', 'Ekra'];

// for (let i = 0; i < friends.length; i++) {
//     const element = friends[i];
//     if (element == 'Mafruha') {
//         return i;
//         // console.log(i);
//     }
// }

const numbers = [100, 80, 30, 60, 10, 60];
for (let i = 0; i < numbers.length; i++) {
    const element = numbers[i];
    if (element == 10) {
        console.log(i);
    }
}